import { styled } from '@/libs/stitches';

export const Badge = styled('div', {
  display: 'inline-block',
  background: '$primaryA',
  color: '$primary',
  padding: '$2xs',
  fontSize: '$xs',
  fontWeight: 'bold',
  borderRadius: '$1',
  lineHeight: 1,
});
